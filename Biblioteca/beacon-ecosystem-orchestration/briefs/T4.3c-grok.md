# T4.3c — ECG source driver for harmonic-weaver (rhythmic trigger)

Agent: grok. Size: M. Wave: 4.
Repos: read from ~/Projects/cymatic-control; deliver ONE new file in ~/Projects/harmonic-weaver.
Specs (READ FIRST):
- ~/Projects/cymatic-control/ecg_analysis.py — `ECGProcessor` with Pan-Tompkins R-peak detection (has its own tests in ~/Projects/cymatic-control/tests/). Wrap, don't rewrite.
- ~/Projects/cymatic-control/README.md + config.json — the ESP32 chest sensor firmware sends OSC to `:5001`; find the exact address scheme (also check firmware/ if needed).
- ~/Projects/harmonic-weaver/contracts/source_frame.template.json + docs/CORE_DESIGN.md §3 (rising-edge ECG routes discard invalid/held triggers and never queue events).
- Simulators for testing: ~/Projects/cymatic-control/ (find `simulate_*` scripts or the test fixtures).

## Task

Create `src/harmonic_weaver/drivers/ecg_driver.py`:

1. Wrap `ECGProcessor` as a weaver source: receive the ESP32 chest-sensor stream (OSC :5001, exact addresses from the cymatic-control code), feed samples to the processor, expose channels: `ecg.beat` (rising-edge trigger event), `ecg.bpm` (continuous, smoothed), `ecg.signal_quality` (0–1 if available from the processor — check its API).
2. Trigger semantics per CORE_DESIGN: beat events are edge-triggered, never queued, and dropped (not held) when the source is held/invalid.
3. Source Frame v1 states: stream alive → `observed`; sensor dropout → channels `invalid` after the lease; BPM may be `held` briefly between beats.
4. Emits via `on_frame(source_id, channel_values)` like the other drivers.
5. `tests/test_ecg_driver.py`: synthesize a synthetic ECG (known BPM, e.g. 60 and 120, with noise) — or reuse cymatic-control's existing test approach/fixtures — feed it through the driver and assert: detected BPM within tolerance, beat events emitted as edges (count ≈ expected beats), invalid propagation on dropout. Copy/adapt the ECGProcessor code into the driver module (vendored, with source note) or import it by file path — prefer vendoring the minimum needed to keep the weaver dependency-free.

## Constraints

- **Do NOT commit.** ONLY this file + its test. Do not touch shared files (concurrent agents in the repo).
- English.

## Report

OSC input scheme found, processor API used, channel semantics, test output with BPM tolerance.
