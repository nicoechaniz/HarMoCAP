# T4.3a — HarMoCAP source driver for harmonic-weaver

Agent: grok. Size: M. Wave: 4.
Repos: read from ~/Projects/HarMoCAP (the kit); deliver ONE new file in ~/Projects/harmonic-weaver.
Specs (READ FIRST):
- ~/Projects/HarMoCAP/harmocap-nico-kit/ (README.md, osc_codec.py, INTERFACE_SPEC.md, examples/) — the portable receiver kit; codec and replay are already solved there.
- ~/Projects/harmonic-weaver/contracts/source_frame.template.json (T1.1) and ~/Projects/harmonic-weaver/docs/CORE_DESIGN.md §2 (registry addressing: channels as `source_id.channel`).
- Fixture for testing: ~/Projects/HarMoCAP/harmocap-nico-kit/examples/ (find the `two_persons.jsonl` replay fixture — if not in the kit, check ~/Projects/HarMoCAP/ root-level fixtures).

## Task

Create `src/harmonic_weaver/drivers/harmocap_driver.py` — a source driver that:

1. Receives the HarMoCAP OSC 1.1 stream (per-person atomic bundles, `/harmocap/v1/*`) using the kit's codec approach (pure stdlib; you may copy the minimal decode functions needed into the driver or import the kit by path — prefer copying to keep the weaver dependency-free; if you copy, note the source file+hash).
2. Exposes slots/keypoints/features as weaver source channels following Source Frame v1 and CORE_DESIGN §2 addressing, e.g. `harmocap.slot_3_keypoint_left_wrist_x`, `harmocap.slot_3_kinetic_energy`, `harmocap.slot_0_focused`. Channel granularity: all 17 keypoints (x, y, conf), all 21 features, present/focused per slot, 8 slots.
3. Implements the contract's receiver rules: hello/calibration gating on (contract_id, calibration_generation, calibration_hash), monotonic discard per stream_id, lease expiry (2 s) marking channels invalid, tombstone handling (present=0), observed|held|invalid propagation from feat_state/kp_state.
4. Emits frames into a callback interface: `on_frame(source_id, channel_values: dict[str, tuple[value, state, confidence]])` (the engine side is T4.2 — code against this seam, documented in the module docstring).
5. Tests `tests/test_harmocap_driver.py`: replay the two-persons fixture through the driver (UDP loopback or direct packet feed — prefer feeding raw datagrams to avoid port flakiness) and assert: channels appear for both slots with correct names; focus migration mid-stream is reflected; a stale stream_id resets counters; held/invalid states propagate.

## Constraints

- **Do NOT commit.** Create ONLY this one file + its test. Do not touch `__init__.py`, pyproject, or other drivers' files — the orchestrator wires the package (another agent works in this repo concurrently).
- English.

## Report

What you copied vs referenced from the kit, channel naming table (sample), test output.
