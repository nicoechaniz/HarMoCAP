# T4.3b — MIDI source driver for harmonic-weaver (recovery from exhausted Kimi run)

Agent: codex (`gpt-5.6-sol`, xhigh). Size: S. Wave: 4 recovery.
Repo: ~/Projects/harmonic-weaver.

## Prior failed attempt

Kimi exhausted its quota while writing this task. It left an **uncommitted partial** file at:
`src/harmonic_weaver/drivers/midi_driver.py`

There is **no test file** and nothing from that partial attempt is accepted. Inspect it; complete it if sound, or replace it. Do not preserve bad assumptions merely because the file exists.

## Specs (read first)

- `~/Projects/cymatic-control/midi_relay.py` — existing MIDI relay (CC/note → normalized OSC).
- `~/Projects/cymatic-control/config.json` — current device/channel conventions.
- `~/Projects/harmonic-weaver/contracts/source_frame.template.json`
- `~/Projects/harmonic-weaver/docs/CORE_DESIGN.md` §2.
- Existing sibling driver seam: `~/Projects/harmonic-weaver/src/harmonic_weaver/drivers/harmocap_driver.py` and `ecg_driver.py`.

## Task

Create/finish exactly these two artifacts:

1. `src/harmonic_weaver/drivers/midi_driver.py`
2. `tests/test_midi_driver.py`

The driver must:

- Generalize the existing relay using `mido` + `python-rtmidi`, mapping CC/note events to normalized Source Frame v1 channels: `midi.cc_<n>`, `midi.note_<n>_vel`, plus semantic aliases such as `midi.modwheel`.
- Support Minilab3 and Launchpad Mini conventions discovered in cymatic-control.
- Be hot-plug tolerant: absent device emits `invalid` channels rather than crashing; arrival transitions usable channels to `observed`.
- Emit using the existing shared seam:
  `on_frame(source_id, channel_values: dict[str, tuple[value, state, confidence]])`.
- Use lazy mido imports so module import remains viable if MIDI dependencies are unavailable.

Tests must use a fake backend/ports (no real hardware), cover normalized CC and note data, aliases, note-on zero velocity release behavior, device absence/arrival/removal, and state transitions.

## Constraints

- Do NOT commit.
- Do NOT modify `__init__.py`, `pyproject.toml`, any sibling driver, or other files.
- English only.

## Report

Channel map, device conventions discovered, whether the partial Kimi file was retained/replaced, and full test output.
