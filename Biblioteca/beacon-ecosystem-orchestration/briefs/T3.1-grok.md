# T3.1 — Migrate resonant_filter + sample_layer into beacon-spatial (vendorize nh_analysis dependency)

Agent: grok. Size: M. Wave: 4.
Repos: read from ~/Projects/digital-beacon; deliver in ~/Projects/beacon-spatial.
Spec: ~/Projects/HarMoCAP/Biblioteca/rearquitectura-ecosistema-beacon/reportes_agentes/04-digital-beacon.md §2 (A and B — "lo más limpio y portable del repo", "rescate directo").

## Task

1. Copy `digital_beacon/resonant_filter.py` and `digital_beacon/sample_layer.py` into beacon-spatial under a new `nature/` package (`nature/__init__.py`, `nature/resonant_filter.py`, `nature/sample_layer.py`).
2. `resonant_filter.py` imports `nh_analysis.mask.harmonic_mask` (its ONLY non-stdlib/third-party coupling — from digital-beacon's archived `packages/`). **Vendorize it**: copy the minimum necessary code from `~/Projects/digital-beacon/packages/nh-analysis/` (find the `harmonic_mask` implementation and its transitive deps within nh_analysis) into `nature/_vendor/nh_mask.py`, and rewrite the import. No dependency on `packages/` may remain.
3. Fix imports so both modules run inside beacon-spatial. Declare real third-party deps (numpy, librosa, python-osc — check actual imports) in `requirements.txt`.
4. `tests/test_nature.py`: import tests; harmonic/residual separation on a synthetic signal (sum of harmonics on a known f1 + noise): assert the harmonic component energy concentrates on the f1 lattice and the residual does not; SampleLayer loads a small generated WAV (synthesize one in the test, ~1 s, in /tmp) and produces a SampleDescriptor with sane descriptor values (rms>0, f0 near the synthesized pitch, no NaN).
5. Do NOT wire anything into beacon.scd or OSC — that is T3.2. This task ends with the modules importable and tested.

## Constraints

- **Do NOT commit.** Working tree only; do not modify digital-beacon.
- If the actual code diverges from the report, code wins — flag it.
- English.

## Report

What was vendorized (original path → nature/_vendor/), remaining third-party deps, test output, any surprises in SampleDescriptor fields.
