# T4.3b — MIDI source driver for harmonic-weaver

Agent: kimi. Size: S. Wave: 4.
Repos: read from ~/Projects/cymatic-control (fresh clone of AlterMundi/cymatic-control); deliver ONE new file in ~/Projects/harmonic-weaver.
Specs (READ FIRST):
- ~/Projects/cymatic-control/midi_relay.py — the existing MIDI relay (CC/note → normalized OSC). This is what you generalize.
- ~/Projects/cymatic-control/config.json — current channel/device mapping conventions.
- ~/Projects/harmonic-weaver/contracts/source_frame.template.json and docs/CORE_DESIGN.md §2.

## Task

Create `src/harmonic_weaver/drivers/midi_driver.py`:

1. A MIDI source driver generalizing midi_relay.py: listen to a MIDI device (mido + python-rtmidi), map CC and note events to normalized 0–1 channels named per Source Frame v1: `midi.cc_<n>`, `midi.note_<n>_vel`, `midi.modwheel`, etc. Support the Minilab3 and Launchpad Mini (both present in this ecosystem; check midi_relay + cymatic config for their conventions).
2. Hot-plug tolerant: device absence = channels `invalid` (never crash); device arrival = channels go `observed`.
3. Emits via `on_frame(source_id, channel_values: dict[str, tuple[value, state, confidence]])` — same seam as the other drivers.
4. `tests/test_midi_driver.py`: use mido's virtual ports (or a fake backend) to feed CC/note events and assert normalized channels with correct names/ranges/states. No real MIDI hardware on this machine — do not require it.

## Constraints

- **Do NOT commit.** Create ONLY this one file + its test. Do not touch `__init__.py`, pyproject, or other files (concurrent agents in the same repo).
- If mido/python-rtmidi is not installed here, write the code so imports are lazy (driver degrades to `invalid` channels without the lib) and note it.
- English.

## Report

Channel map, device conventions found in cymatic-control, test output.
