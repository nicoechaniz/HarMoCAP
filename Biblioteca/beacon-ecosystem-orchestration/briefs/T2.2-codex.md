# T2.2 — Extract the Shaper into harmonic-shaper (fork reconciliation)

Agent: codex (gpt-5.6-sol, xhigh). Size: L. Wave: 4 (critical path).
Repos: read from ~/Projects/digital-beacon and ~/Projects/NaturalHarmony; deliver in ~/Projects/harmonic-shaper (scaffold + T1.3 manifest already merged).
Specs (READ FIRST):
- ~/Projects/HarMoCAP/Biblioteca/rearquitectura-ecosistema-beacon/reportes_agentes/04-digital-beacon.md §1, §3, §6 (what to extract, fork differences, coupling points).
- ~/Projects/harmonic-shaper/contracts/shaper.contract.json (T1.3 — the OSC surface you must end up exposing; the code-wins discrepancies documented there are binding).
- ~/Projects/HarMoCAP/Biblioteca/rearquitectura-ecosistema-beacon/SINTESIS.md §3.B (decision: digital-beacon version is canonical; reconcile the fork into ONE source of truth).

## Task

Migrate the Shaper into a standalone, installable, tested package.

### Extract from digital-beacon (`digital_beacon/`)

- `audio_engine.py` (232 lines) — the 32-voice additive engine.
- `state.py` (380) — VoiceParameterStore.
- `config.py` — only the shaper-relevant sections (f1, 32 bands, MAX_VOICES, ports).
- `midi_control.py` (404) — Launchpad Mini + Minilab3.
- `osc_receiver.py` — the `/digital/*` branch (:9002) AND the `/beacon/*` slave broadcast branch (:9001). Keep both listeners; the slave mode must become OPTIONAL via flag (see below).
- The `/api/shaper/*` endpoints from `api.py` + the WebSocket `/ws` state broadcast — extract the shaper-relevant parts into a standalone FastAPI app (this is the current state_sync mechanism documented in the T1.3 manifest).
- `tools/synth_pure.py` — the numpy-pure reference implementation (golden reference for the clipping diagnosis in T2.4).

### Reconcile with NaturalHarmony

- The original is `NaturalHarmony/harmonic_shaper/` (5 voices). digital-beacon's is the evolution (32 voices, waveshaper, per-voice LFO, sidechain, recording tap, 1/√N normalization). Decision is made: digital-beacon version wins. Your reconciliation duty is only to check the original for anything of value that was LOST in the fork (read it; if nothing, say so explicitly in the report).

### Target state

- Package `harmonic_shaper` under `src/` (existing pyproject, src-layout). Clean module structure, real dependencies declared in pyproject (`sounddevice`, `numpy`, `python-osc`, `fastapi`, `uvicorn`, `mido`/`python-rtmidi` as applicable — check actual imports).
- `python -m harmonic_shaper` (or an entry point) starts the synth standalone: engine + OSC listeners + web API. Slave broadcast mode behind `--slave` flag (default: standalone, per T2.3 direction; the broadcast listener stays available).
- Rename internal references: config/ports/docs should speak `/shaper/*` as the PLANNED namespace while remaining wire-compatible with the existing `/digital/*` addresses (the manifest documents reality — do NOT rename OSC addresses in code in this task; that's a later, contract-bumping change).
- `pip install -e .` clean.

### Tests (minimum bar)

- Import-level tests of every migrated module (no audio device on this machine — mock/headless where needed).
- State store: thread-safety smoke test (concurrent readers/writer on VoiceParameterStore).
- A render test using synth_pure-style numpy math (not sounddevice): render N samples of a voice, assert no NaN and peak <= 1.0.
- Contract test from T1.3 must still pass as-is.

## Constraints

- **Do NOT commit** in any repo. Deliver in harmonic-shaper's working tree; do not modify digital-beacon or NaturalHarmony.
- No redesign of the engine. Extraction + packaging + minimal glue only.
- English.

## Report

Module map (what landed where), anything valuable found in the NaturalHarmony original or explicit "nothing lost", dependency list vs what the code actually imports, test output, and any clipping-suspicious spots you noticed while reading the engine (input to T2.4).
